# Memoir M8 语音与配乐开发计划

> 2026-09-07 设计，2026-09-08 收口；实施未开始。按复选框记录真实进度，不能用历史测试勾选新任务。唯一跨工程入口为 [Business 回忆录总控](/Users/yuye/YeahWork/Python项目/couple-diary-doc/头脑风暴/docs/superpowers/回忆录/plans/2026-08-06-回忆录-总控开发计划.md)，本仓入口为 [Runtime 总控](../plans/2026-07-07-AgentRuntime-总控开发计划.md)。

## 0. 输入资料

实现前必须阅读（以下源码路径以 `/Users/yuye/YeahWork/AIAgent项目/com-agent-runtime` 为根）：

- `AGENTS.md`、`.codex/rules/AI通用编码与协作规范.mdc`、`.codex/skills/agent-runtime-session/SKILL.md`
- `README.md`、`ENV_CONFIG.md`、`VERIFICATION.md`、`pyproject.toml`
- `头脑风暴/docs/AgentRuntime/需求设计文档.md`、`头脑风暴/docs/AgentRuntime/契约冻结记录.md`
- `头脑风暴/docs/AgentRuntime/plans/2026-07-07-AgentRuntime-总控开发计划.md`
- `头脑风暴/docs/AgentRuntime/backend/2026-07-07-AgentRuntime-后端开发计划.md`
- `头脑风暴/docs/AgentRuntime/plans/2026-08-20-回忆录媒体通道设计说明.md`
- `头脑风暴/docs/AgentRuntime/plans/2026-09-07-Memoir语音与配乐设计说明.md`
- `app/agents/memoir_agent/runner.py`、`app/agents/memoir_agent/1.0.7/agent.yaml`、`app/agents/memoir_agent/1.0.7/tools.manifest.json`（其余包文件以目录实际清单为准）
- `app/services/memoir/memoir_media_service.py`、`app/utils/aliyun/oss_client.py`、`app/worker.py`、`app/runtime/tool_gateway.py`
- `app/models/runtime.py`、`app/models/__init__.py`、`alembic/env.py`、`app/core/config.py`、`app/scripts/agent_runtime_cli.py`
- `docker-compose.yml`、`docker-compose.test.yml`、`docker-compose.production.yml`、`docker/backend/Dockerfile`、`docker/backend/DOCKER_DEPLOY.md`
- `.env.example`、`docker/backend/test.env.example`、`docker/backend/production.env.example`；不读取真实私有env
- `tests/test_memoir_media_channel.py`、`tests/test_memoir_snapshot_runner.py`、`tests/test_memoir_publish_audit.py`、`tests/test_memoir_agent_e2e.py`
- `tests/runtime_test_memoir_package_versions.py`、`tests/runtime_test_memoir_106_full_graph.py`、`tests/test_runtime_contract_compatibility.py`、`tests/test_runtime_agent_package_loader.py`
- `tests/test_config.py`、`tests/test_docker_deployment_contract.py`、`tests/fixtures/memory-runtime-contract-v1.1.0.json` 与当前 `app/contracts/` 清单、Alembic head
- [Business 产品文档](/Users/yuye/YeahWork/Python项目/couple-diary-doc/头脑风暴/docs/superpowers/回忆录/需求设计文档.md)、[共享技术设计](/Users/yuye/YeahWork/Python项目/couple-diary-doc/头脑风暴/docs/superpowers/回忆录/designs/2026-09-07-回忆录语音播报与背景音乐设计说明.md)、[Business B17–B21](/Users/yuye/YeahWork/Python项目/couple-diary-doc/头脑风暴/docs/superpowers/回忆录/backend/2026-09-07-回忆录-M8语音与配乐-后端开发计划.md)、[前端 F13–F16](/Users/yuye/YeahWork/Python项目/couple-diary-doc/头脑风暴/docs/superpowers/回忆录/frontend/2026-09-07-回忆录-M8语音与配乐-前端开发计划.md)

## 1. 成功标准与现状

实际最新包1.0.7，仅图文媒体；`memory.enqueue_tts`禁用、图片public-read、媒体恢复重算均为代码事实。M7部署通过是用户确认，M8没有实现证据。新包1.0.8在审核后生成完整场景旁白和每作品一首音乐，失败有界降级；不修改旧包/公共消息调度，不建立Business作品事实表。

| 有效需求/验收 | Runtime交付 | 状态 |
|---|---|---|
| 回忆录/REQ/AC-022 | 全文TTS、分段完整、固定女声 | R6、R8待开发 |
| 回忆录/REQ/AC-023 | 每作品一首BGM、两类音频私有OSS | R6、R8待开发 |
| 回忆录/REQ/AC-024 | 提供独立音轨元数据和真实时长；播放由前端负责 | R8待开发 |
| 回忆录/REQ/AC-025 | Memoir专属边界、配置与新包/旧wire | R6–R8待开发 |
| 回忆录/REQ/AC-026 | 部分失败、预算、提交未知与恢复 | R7、R8待开发 |
| 回忆录/REQ/AC-027 | Business资格选包；Runtime旧包不改 | R8回归，资格实现归B18 |
| 回忆录/REQ/AC-028 | private上传、scope、孤儿对账；发布后归Business | R6–R8待开发 |

## 2. 文件边界与所有权

下列均为计划创建，当前不存在：

- `app/services/memoir/memoir_audio_service.py`：场景/作品音频协调及预算检查。
- `app/services/memoir/memoir_audio_provider.py`：TTS SSE与音乐提交查询adapter，拒绝敏感日志。
- `app/services/memoir/memoir_audio_storage.py`：private OSS上传、转码临时文件与有界下载。
- `app/services/memoir/memoir_audio_jobs.py`：唯一槽、费用预留、lease/fencing、对账。
- `app/models/memoir_audio_job.py`：执行元数据模型；按现有元数据入口注册，不新增作品事实。
- `app/scripts/memoir_audio_maintenance.py`：只维护Memoir音频作业和未发布对象。
- `alembic/versions/20260907_1000_add_memoir_audio_jobs.py`：实施时检查revision及head无冲突，只迁移Runtime库。
- `app/agents/memoir_agent/1.0.8/`：版本化包及其配置资产；完整文件清单在复制前由主Agent按1.0.7目录冻结。
- `tests/test_memoir_audio_provider.py`、`tests/test_memoir_audio_jobs.py`、`tests/test_memoir_audio_storage.py`、`tests/test_memoir_audio_migration.py`、`tests/runtime_test_memoir_108_full_graph.py`。
- `tests/fixtures/memory_playback_shared_v2.json`：主Agent独占，与Business后端同名文件逐字对齐；旧fixture不覆盖。

Runtime Agent独占以上新文件和R6–R8列出的既有本仓文件；主Agent独占跨仓fixture、契约/设计/总控。所有Agent必须知道自己不是唯一工作者，不回退/覆盖他人改动；新增共享文件先报告，poetry.lock等按owner串行。Business与Frontend文件不可由Runtime Agent抢改。

## 3. 契约与配置冻结

执行 [Runtime设计](../plans/2026-09-07-Memoir语音与配乐设计说明.md)§2–5和Business主设计§4–6：Playback2新增audio对象、旧图片manifest保留、资源真实时长、无签名URL、幂等键含scene_id、scope标准序列化。新包映射memory wire1.1.0，公共API/Tool/callback envelope不改。

配置具体文件及变量由 [ENV_CONFIG M8](../../../../ENV_CONFIG.md)持有。必须交付Settings、三个env模板、Compose env_file链验证和Dockerfile转码工具：

- 语音Key在官方 `https://console.volcengine.com/speech/new/setting/apikeys` 创建，填 Runtime 私有env `MEMOIR_TTS_API_KEY`；本地`.env.development.local`仅开发者填写，沿现有按环境加载机制，不使用不会被加载的根`.env`。音乐用`VOLCANO_CV_ACCESS_KEY/SECRET_KEY`经独立服务开通/IAM授权，不需要第二个语音Key。
- 正式背景`memoir/audios/background/`、旁白`memoir/audios/narrator/`；测试背景`memoir-test/audios/background/`、旁白`memoir-test/audios/narrator/`。两仓`MEMORY_AUDIO_BACKGROUND_PREFIX`/`MEMORY_AUDIO_NARRATOR_PREFIX`同值。
- 运维真实env沿`RUNTIME_ENV_FILE`，示例为`/usr/HokageYeah/服务端系统/env/runtime-test.env`和`runtime-production.env`。`docker-compose.yml`实际是`x-runtime-env-file`注入，各进程共用；不是Business的CD_插值模型。
- `docker/backend/Dockerfile`在切换非root前安装ffmpeg/ffprobe，不以ARG或ENV写Key。`app/core/config.py`条件验证开启音频所需Key/单价/预算/桶/前缀/host清单；默认关闭不影响其他Agent。

## 4. 数据与状态机

账本字段、唯一键、状态和成本并发按Runtime设计§5。必须以实际SQLAlchemy/数据库兼容实现唯一槽：scene/segment空值用明确哨兵，不能在PostgreSQL/SQLite上误以为NULL唯一约束会拒绝重复。费用使用Decimal，完整输入保守预留并按实际text_words/requested music seconds结算，未知结果不释放预算；无可证上界不提交。通用AgentModelUsage不加audio字段，不把audio字符当token。

上传前固定对象键，崩溃后可对账；旧lease不写新结果。恢复只复用已知完整资源/音乐TaskID，不因临时分段文件丢失重复收费；submission_unknown降级。音频维护命令先publish query确定是否已发布，已发布移交Business、未知保留，明确未发布才按保留策略清理。

迁移仅`couple_diary_agent_runtime_*`，不访问或迁移Business库。upgrade接当前单head；downgrade只在隔离环境无在途任务/费用待对账/未清理对象时演练，生产回滚保留账本。既有规范里的历史业务表不恢复使用。

## 5. 任务拆分

### [✅] Task R6：Provider、转码和私有OSS

前置：M8-C冻结fixture/共享字段；关联AC-022、023、025、028。修改`app/core/config.py`；新建audio_provider/audio_storage及对应tests。优先使用现有异步HTTP依赖与OSS SDK，若确需新增依赖才改pyproject/lock并说明原因；不修改public-read图片上传行为。

- [✅] Step 1：先用模拟HTTP写TTS帧/成功终态/断流/坏Base64/取消/空音频与音乐提交/查询状态测试；相同正文不同scene资产ID独立。
- [✅] Step 2：实现固定TTS协议、按句无损分段、普通话同音色、完整MP3拼接及真实时长校验；限制临时权限、下载大小、转码超时及取消。
- [✅] Step 3：实现显式音乐Action、独立signature region/service、TaskID查询、精确host与SSRF/重定向限制；private ACL上传、角色/环境scope路径校验。
- [✅] Step 4：覆盖private上传失败、错误mime/解码、超限、官方URL未入任何持久化/日志；现有图片媒体测试不变。

验证：`poetry run pytest tests/test_memoir_audio_provider.py tests/test_memoir_audio_storage.py tests/test_memoir_media_channel.py -q`。Checkpoint：模拟证明协议/降级/私有上传方法隔离；真实Key权限、试听和OSS ACL仍由R8现场验收，不用mock宣称通过。

### [✅] Task R7：作业幂等、预算、恢复与孤儿清理

前置：R6；关联AC-026、028。新建audio_jobs/model/migration/maintenance及jobs/migration tests；修改`app/models/__init__.py`和`alembic/env.py`所需模型注册（若当前自动扫描已覆盖则只测试，不重复注册）。不改公共worker状态机。

- [✅] Step 1：先写真实事务接缝测试：同Run同输入并发、重复正文不同scene、未知提交、已知TaskID、过期lease及费用并发预留。
- [✅] Step 2：迁移安全元数据表及约束/索引，记录上传前稳定key；实现原子预留/结算/状态转移和fencing，禁止明文正文、URL/音频字节落表。
- [✅] Step 3：实现按Run剩余预算/取消/隐私授权门禁，未知提交不重付费；分段临时资产丢失时降级；所有失败保留成功完整场景。
- [✅] Step 4：新增维护命令 `python -m app.scripts.memoir_audio_maintenance --environment test --dry-run`，执行删除需将dry-run替换为`--execute`；environment仅test/production，二者不能同时指定，未指定模式默认dry-run，`--limit`默认100限制每批。按安全ID列出候选计数，调用现有publish query核验，未知保留、已发布移交、明确未发布且超过`MEMOIR_AUDIO_ORPHAN_RETENTION_HOURS=24`才删除。404幂等；无bucket/全Archive通配删除选项；未结算账本行不得清除。
- [✅] Step 5：隔离数据库upgrade/downgrade、SQLite与PostgreSQL唯一槽/费用竞争验证；明确无业务库连接。将已冻结CLI参数与真实验证结果同步ENV_CONFIG/VERIFICATION；以上CLI为待创建合同，本轮不能当现有命令运行。

验证：`poetry run pytest tests/test_memoir_audio_jobs.py tests/test_memoir_audio_migration.py -q`、`poetry run alembic heads`；PostgreSQL使用现有harness规范，在隔离库运行同一行为测试。Checkpoint：不会因恢复/并发重复扣预算或盲目再次收费；无已发布资源被误删；所有敏感数据扫描通过。

### [ ] Task R8：新包集成、配置部署与全链路回归

前置：R6、R7、B17/B18 reader/资格与M8-C冻结；关联全部AC-022–028。修改`app/agents/memoir_agent/runner.py`、`app/worker.py`、`app/runtime/tool_gateway.py`；新建1.0.8包与full_graph测试；修改版本/loader/兼容测试、config/deployment tests、三个env模板、Dockerfile和必要Compose文件。文档更新README/ENV_CONFIG/VERIFICATION及本计划，本仓总控由主Agent同步。

- [✅] Step 1：先冻结旧包文件SHA/digest和公共wire回归，注册新包到既有wire；只在新包审核后执行音频。旧版本仍图片/no-opTTS且digest不变。（2026-09-08：1.0.0–1.0.7 digest 冻结断言进 runtime_test_memoir_package_versions.py / test_runtime_agent_package_loader.py；wire 登记表补 1.0.8→1.1.0 并加 8 版本全覆盖断言；音频只在新包 1.0.8 图执行，旧包字节零变化）
- [✅] Step 2：注入Memoir服务并对同Run/epoch恢复重取授权素材，生成后body不可再改；发布完整图文及成功audio，失败不另起补音revision。（2026-09-08：Worker 注入 configured_audio_service；memoir_audio_service 按 (run,epoch,role,scene,segment,input HMAC) 幂等复用已上传资产、在途音乐凭 TaskID 只查不重建；音频节点只读 safety_review 冻结正文，任何失败有界降级且发布单次幂等）
- [✅] Step 3：Run1200秒、音频300秒、发布预留30秒与单请求剩余deadline一致；全Worker信号量及成本独立封顶。测图文耗尽预算、部分音频/全失败、取消与迟到回调无新写入。（2026-09-08：1.0.8 agent.yaml max_run_seconds=1200；音频预算=min(节点300s, Run 剩余, 墙钟剩余)−30s；场景信号量2+进程信号量4包住每次供应商调用；音频成本走 MemoirAudioRunBudget 原子封顶；四个场景均有 108 全图测试）
- [✅] Step 4：按ENV_CONFIG逐项实现Settings/模板/env_file/Dockerfile，纯占位Compose验证不输出真实env；构建镜像验证非root可运行ffmpeg/ffprobe并可清理临时目录。（2026-09-08：Settings/校验（R6）+三模板占位+Dockerfile apt ffmpeg 已落地并有静态合同测试；Compose env_file 链无密钥断言进 test_docker_deployment_contract.py。镜像构建与非 root ffmpeg 实机验证因本机 Docker daemon 未运行未执行，留运维部署演练，未用 mock 宣称通过）
- [ ] Step 5：实际测试环境开通服务与单位价格/额度证据、官方音乐下载host、真实短样本语音/配乐、四前缀上传、匿名拒绝/签名成功；不把厂商临时URL交前端。
- [ ] Step 6：与Business和Frontend验证全成功/部分/无音频、新旧作品/客户端、owner/visitor授权、撤分享/删除/隐私撤销；微信双音轨由F16真机验收，未通过不启生产资格。

Checkpoint：所有本轮命令有结果、旧包/公共合同不变、两仓fixture一致、配置落点可执行；缺真实服务或真机证据保留R8/M8-V未完成。注册部署由运维阶段执行，本计划不自动授权生产操作。

## 6. 验证命令

以下在Runtime根执行，新增tests须对应任务创建后运行：

```bash
poetry run pytest tests/test_memoir_audio_provider.py tests/test_memoir_audio_storage.py tests/test_memoir_audio_jobs.py tests/test_memoir_audio_migration.py tests/runtime_test_memoir_108_full_graph.py -q
poetry run pytest tests/test_memoir_media_channel.py tests/test_memoir_snapshot_runner.py tests/test_memoir_publish_audit.py tests/runtime_test_memoir_package_versions.py tests/runtime_test_memoir_106_full_graph.py tests/test_runtime_contract_compatibility.py tests/test_runtime_agent_package_loader.py tests/test_config.py tests/test_docker_deployment_contract.py -q
poetry run ruff check app tests
poetry run mypy app
poetry run alembic heads
git diff --check
```

原有pytest配置收集`test_*.py`及`runtime_test_*.py`。发布门禁按现有VERIFICATION/CI要求扩大到完整测试、隔离PostgreSQL/Redis/Docker harness；不为了运行真实服务把密钥写fixture。两仓共享新fixture字节比较由主Agent完成。本轮只写文档，未运行上述新增测试、构建或真实迁移。

## 7. 工作流、风险与交付

先adaptive-workflow，使用总控同一个计划执行流程；在provider/事务/状态边界编写行为测试，完成后code-review双轴。无插件时使用本计划原生命令，不伪造CCG。所有Agent按文件所有权工作，不在同一runner/lockfile并行抢改。

- [ ] 交付新包、专属服务/模型/迁移/维护工具与测试、配置模板/镜像依赖、旧包digest和新fixture对比证据。
- [ ] 交付真实账号开通/用量口径/私有OSS与音色试听证据，记录仍需人工项且不粘贴秘密。
- [ ] 主Agent同步R6–R8/M8-V进度和评审结论；本轮设计日期保留，实施记录另写真实日期。

非目标和回滚同Runtime设计§6/ENV_CONFIG M8：不改公共消息/调度，不补旧作品，不预混两轨、不后台持续播放、不自动提交/部署。外部能力冲突必须报告用户确认；缺资料/账号配置不能以0费用、默认公开ACL或静默替代服务绕过。

## 8. 实施记录（主Agent门禁，真实日期）

### 2026-09-08 R6 验收通过（门禁复核）

- 独立复跑：`pytest tests/test_memoir_audio_provider.py tests/test_memoir_audio_storage.py -q` = 48 passed（当日口径；后续 R7 门禁补回归测试后为 49）；`ruff check app tests` 通过。
- 白名单核对无越界；图片媒体公共上传行为未改动。

### 2026-09-08 R7 门禁通过

- 白名单核对：仅 R6/R7 归属文件在工作区（`config.py`/`models/__init__.py`/五个 audio 新文件/四个测试/迁移/fixture），无越界改动。
- 独立复跑结果：jobs+migration `pytest -q` = **33 passed, 1 skipped**（skip=PostgreSQL harness 未提供 DSN，红线豁免项）；R6 回归 provider+storage = **49 passed**；`ruff check app tests` 通过；`alembic heads` 单 head `20260907_1000`；`git diff --check` 清洁。
- **mypy 门禁与诚实归因**：初跑 23 错。其中 17 错位于 5 个**未修改的已提交文件**（`agent_runtime_cli.py`×2、`bounded_loop.py`×2、`register_agent_package.py`×5、`executor.py`×1、`memoir_media_service.py`×7），为 M5–M7 时期存量债（M3 收口时 mypy 曾全绿），mypy 不在 pre-commit，按既有先例记录为非阻断，不顺手修。本轮修复：
  1. `app/scripts/memoir_audio_maintenance.py` 5 错——`OssDeleter` 原声明为 `Callable[[str], bool]` 与全部用法（方法调用/类实现/测试假件）矛盾，改为 `Protocol`（`delete_object(object_key) -> bool`）；OSS SDK 懒加载句柄由 `object` 改为 `Any`。
  2. `app/services/memoir/memoir_audio_storage.py` **真实运行期缺陷**（R6 引入，mypy 发现）：真实 DNS 分支误写 `info[0][4][0]`，对 `AddressFamily` 枚举取下标，`resolver=None` 生产路径必崩 `TypeError`；既有测试全部注入 resolver 绕开该分支。已改 `str(info[4][0])` 并新增回归测试 `test_download_real_dns_path_reads_sockaddr_first_element`（monkeypatch getaddrinfo 走完整下载入口，旧代码在该测试下必败）。
- 修复后 `mypy app` = 17 错全部为上述存量债，M8 工作区文件零错误。
- CLI 合同已按 Step 4/5 落地并同步 ENV_CONFIG/VERIFICATION；真实 OSS 删除、生产保留窗调优留待 R8/M8-V 人工验收，未用 mock 宣称通过。

### 2026-09-09 R8 门禁通过（Steps 1–4 实施与全量回归；Steps 5–6 待真实服务/真机证据）

- 白名单核对：本轮改动恰 3 文件（桥接测试前缀对齐 + 网关消费表补登记 + 新增回归测试），其余工作区改动均为 R6–R8 既有归属文件，无越界。
- 独立复跑：R8 相关分批 90 passed/1 skipped + 183 passed；桥接修复后全量 `pytest -q` = **1110 passed, 17 skipped, 0 failed**（skip 均为 PG/Redis 隔离 harness 未显式开启，红线豁免）；`ruff check .` 通过；mypy 17 错/5 文件与 R7 基线一致（M8 工作区零新增）；两仓 `memory_playback_shared_v2.json` SHA-256 `bfd85d9d…412b` 未动；旧包 digest 冻结断言通过。
- 跨仓矩阵核验：`TOOL_ERROR_SPECS_V1_1` 三新码（M8 音频两码 + `MEMORY_DOCUMENT_MEDIA_INVALID`）四字段与 Business `memory_agent_tools_api.py` 逐字一致；冻结 v1.1.0 fixture 不枚举错误矩阵、无需变更。
- 桥接失败复合根因与修复：桥接造数沿用旧生产前缀 `memoir/images/` 触发业务 422（测试环境自 d47ed9c 起为 `memoir-test/images/`），且该码自 M6 已在业务 v1.1.0 生产矩阵、Runtime 消费表漏登记 → 修复 = 测试前缀对齐 + 消费表补登记 + 新增回归 `test_wire_v1_1_matrix_accepts_business_m6_media_error_code`。v1.0.x 降级在业务生产者侧完成（未知码映射 `MEMORY_SNAPSHOT_UNAVAILABLE` 403），网关逻辑零改动。
- Steps 5–6（真实测试环境服务开通/价格与额度证据、与 Business/Frontend 真机联调）保持 [ ]：按本计划 Checkpoint 缺真实服务或真机证据保留 R8 未完成，剩余项与总控 M8-V/F16 门禁合并验收。总控矩阵 R8 [✅] 对应其交付范围（新包集成/配置/注册/回归 = Steps 1–4，权威记录见总控 L998）；本计划 R8 任务头按自身 Checkpoint 维持 [ ] 直至 Steps 5–6 证据补齐，两者口径不冲突。

### 2026-09-10 第三轮 C1 旁注（不改写行 3 历史文案；不重开 R6–R8）

超时后后台 OSS 仍可能上传成功，持键账本停在 reserved/submitted/processing，不进 `_ORPHAN_STATES`。落地：内层 `TimeoutError` 不 `mark_failed`；`fail_abandoned_keyed_jobs` 条件 UPDATE 收割过窗持键 active（`AUDIO_LEASE_ABANDONED`，不刷新 `updated_at`）；execute 先 reap 再按 `audio_object_keys` 成员关系分类。独立复跑三件套 **78 passed / 1 skipped**。同日兼容补缺：仅显式完整 `list[str]` 参与成员判断，缺字段/非法 → `keep_unknown` 零删除；网关对固定发布路由字段级跳过合法 digest，新增 keys 仍扫描。受影响面 **182 passed / 1 skipped**。R8 Steps 5–6 仍 `[ ]`。完整证据见[第三轮必要补缺完成记录](/Users/yuye/YeahWork/Python项目/couple-diary-doc/头脑风暴/docs/superpowers/回忆录/verification/2026-09-10-M8第三轮必要补缺完成记录.md) §3.1。
