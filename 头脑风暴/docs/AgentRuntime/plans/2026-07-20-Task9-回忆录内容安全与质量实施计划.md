# Task 9 回忆录内容安全与质量 Implementation Plan

> **2026-08-31 版本校准：** 本文 Task 1～3 是 `memoir_agent@1.0.0`～`1.0.3`
> 的历史实施记录，其中 3～8 Scene、16 Scene 发布门和 80 字 Scene body
> 限制不得再解释为当前全局规则。`1.0.4` 已只保留至少 3 Scene，并取消
> Scene 数量及 body 产品上限；`1.0.5` 的五类素材动态生成与通用
> `bounded_loop` 已在当前工作区实现并有测试证据（未注册、未部署），实施记录见本文 Task 4 与
> [通用受控循环与 Memoir 动态生成设计说明](./2026-08-31-通用受控循环与Memoir动态生成设计说明.md)。

> **For agentic workers:** REQUIRED SUB-SKILL: Use `superpowers:subagent-driven-development` (recommended) or `superpowers:executing-plans` to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 将 MemoirAgent 的冻结快照转为最小语义脱敏素材，并在发布前保证 Scene 数量、文案长度、情绪安全和媒体关闭契约。

**Architecture:** `sanitize_materials` 作为静态 deterministic 节点紧接 `load_snapshot`，写入既有 `AgentState.sanitized_material`；高光与模型请求只从其安全 `source_refs` 读取。Scene/Action 约束继续收敛在 `MemoirNodeRunner` 的既有 schema 与 `safety_review`，不新增存储、动态工作流或媒体队列。

**Tech Stack:** Python、Pydantic、pytest、现有 MemoirNodeRunner、AgentState、ContextManager、ToolGateway。

## Global Constraints

- 不记录日记正文、赌局完整内容、prompt、工具原始输出或敏感命中原文。
- 敏感素材仅保留 `source_ref/type/sensitive`，不得提供文本给模型或 Artifact/checkpoint 摘要。
- 不新增 LangChain、媒体任务、数据库表、动态工具选择或后台线程。
- 保留共享脏工作区改动；不 reset/checkout/自动提交。

---

### Task 1: 冻结工作流与语义脱敏节点

**Files:**
- Modify: `app/agents/memoir_agent/1.0.0/workflow.graph.py`
- Modify: `app/agents/memoir_agent/runner.py`
- Test: `tests/test_memoir_snapshot_runner.py`
- Test: `tests/runtime_test_planner.py`

**Interfaces:**
- Consumes: `AgentState.snapshot` 内已由内部 Tool 解密的 diary/bet 项及稳定 `id`。
- Produces: `AgentState.sanitized_material={"materials": [...]}`，每项只含 `source_ref/type/sensitive/summary`；敏感项没有 `summary`。

- [✅] **Step 1: 写失败测试**

```python
def test_sanitize_materials_redacts_identifiers_and_keeps_safe_reference() -> None:
    state = AgentState(snapshot={"diaries": [{"id": "d1", "content": "小明电话13800138000"}]})
    MemoirNodeRunner(object()).run_node({"node_id": "sanitize_materials"}, _run(), state)
    assert state.sanitized_material == {"materials": [{"source_ref": "diary:d1", "type": "diary", "sensitive": False, "summary": "我电话[REDACTED]"}]}


def test_sanitize_materials_drops_sensitive_item_text() -> None:
    state = AgentState(snapshot={"diaries": [{"id": "d1", "content": "私密正文", "sensitive": True}]})
    MemoirNodeRunner(object()).run_node({"node_id": "sanitize_materials"}, _run(), state)
    assert state.sanitized_material["materials"] == [{"source_ref": "diary:d1", "type": "diary", "sensitive": True}]
```

- [✅] **Step 2: 运行红灯测试**

Run: `poetry run pytest -q tests/test_memoir_snapshot_runner.py tests/runtime_test_planner.py -k 'sanitize or memoir_static_plan'`

Expected: FAIL，因为静态图没有节点且 Runner 不支持 `sanitize_materials`。

- [✅] **Step 3: 最小实现**

为静态图插入 `sanitize_materials`。Runner 使用一个私有 `_sanitize_materials(snapshot)`：只读取 list 内 Mapping 的 `id/content/sensitive`，用稳定 `diary:`/`bet:` 引用；正常文本替换昵称为“我/TA”、移除手机号/身份证/邮箱/URL/token 并截断 80 字，敏感标记或发现不可安全解释的字段时不复制文本。日志只记录 run_id、素材计数、敏感计数和标准错误码。

- [✅] **Step 4: 运行绿灯与规划回归**

Run: `poetry run pytest -q tests/test_memoir_snapshot_runner.py tests/runtime_test_planner.py`

Expected: PASS，AgentPlan 顺序包含节点，脱敏状态无私密正文。

---

### Task 2: 高光输入、Scene 数量和安全审核约束

**Files:**
- Modify: `app/agents/memoir_agent/runner.py`
- Test: `tests/test_memoir_snapshot_runner.py`
- Test: `tests/test_memoir_model_gateway.py`

**Interfaces:**
- Consumes: `sanitized_material.materials`、既有 StructuredOutputParser、Scene/Action 状态。
- Produces: 高光只使用非敏感 `source_ref`；`generate_scenes` 输出或 fallback 为 3～8 Scene；`safety_review` 只接受 3～16 Scene、`body` 至多 80 字、无禁用情绪词的文档。

- [✅] **Step 1: 写失败测试**

```python
def test_highlights_ignore_sensitive_material_reference() -> None:
    state = AgentState(sanitized_material={"materials": [{"source_ref": "diary:d1", "type": "diary", "sensitive": True}]})
    MemoirNodeRunner(object()).run_node({"node_id": "extract_highlights"}, _run(), state)
    assert state.highlights == {"source_refs": [], "mode": "template"}


def test_safety_review_replaces_overlong_or_too_many_scenes() -> None:
    state = AgentState(scenes=[{"scene_id": f"s{i}", "scene_type": "summary", "source_refs": [], "body": "x" * 81} for i in range(17)], actions=[])
    MemoirNodeRunner(object()).run_node({"node_id": "safety_review"}, _run(), state)
    assert len(state.playback_document["scenes"]) == 3
```

- [✅] **Step 2: 运行红灯测试**

Run: `poetry run pytest -q tests/test_memoir_snapshot_runner.py tests/test_memoir_model_gateway.py -k 'sensitive_material or overlong or too_many'`

Expected: FAIL，因为高光仍读取原 snapshot，安全审核未校验 body/下限。

- [✅] **Step 3: 最小实现**

高光只从 `_safe_material_refs()` 获得非敏感引用；模型 fallback 用无引用基础 Scene 补足到 3 个。扩展 `_is_safe_playback()`：Scene 数量为 3～16、`body` 缺失或长度不超过 80、无禁止表达、引用仍经过现有 allowlist。无效时 `_base_scenes_actions()` 返回 3 个无引用 `summary` Scene 和对应 `show_card` Action，不复制模型或素材正文。

- [✅] **Step 4: 运行绿灯与模型回归**

Run: `poetry run pytest -q tests/test_memoir_snapshot_runner.py tests/test_memoir_model_gateway.py`

Expected: PASS，脏模型输出与敏感引用只触发安全模板 fallback。

---

### Task 3: 媒体关闭、端到端安全回归与总控

**Files:**
- Modify: `tests/runtime_test_worker_entry.py`
- Modify: `头脑风暴/docs/AgentRuntime/plans/2026-07-07-AgentRuntime-总控开发计划.md`
- Modify: `头脑风暴/docs/AgentRuntime/plans/2026-07-20-Task9-回忆录内容安全与质量实施计划.md`

- [✅] **Step 1: 写失败端到端测试**

```python
def test_memoir_worker_publishes_three_to_eight_safe_scenes_with_empty_media_manifest() -> None:
    document = published_document()
    assert 3 <= len(document["scenes"]) <= 8
    assert document["media_manifest"] == []
    assert "私密正文" not in str(document)
```

- [✅] **Step 2: 运行红灯与最小实现**

Run: `poetry run pytest -q tests/runtime_test_worker_entry.py -k 'safe_scenes or media_manifest'`

Expected: FAIL，旧模板仅产生一张 Scene。

将端到端 fixture 调整为经过 `sanitize_materials` 的最小 snapshot；不添加媒体节点。验证 `media=skipped(capability_disabled)` 仍是冻结计划 fallback，最终 document 固定携带空清单。

- [✅] **Step 3: Ponytail 与全量验收**

Run: `poetry run pytest -q && poetry run ruff check . && git diff --check`

Expected: 全绿；复核不重复实现 ContextManager、StructuredOutputParser、Native Tool、Artifact/Checkpoint。只将已通过测试的 Task 9 条目和媒体关闭粒度条目标记 `[✅]`；不提交。

---

### Task 4: `memoir_agent@1.0.5` 动态内容安全与质量（工作区已实现，未注册/部署）

**Files:**
- Create: `app/agents/memoir_agent/1.0.5/*`
- Modify: 通用 AgentPackage schema/loader、bounded loop runner、Memoir evaluator
- Test: bounded loop 与 Memoir 1.0.5 的 package/runner/e2e 回归

**Interfaces:**
- Consumes: Snapshot Tool v1 五类 canonical material 中的安全 `sanitized_payload.text_digest`；无安全摘要的素材只提供受控类型存在性，不向模型暴露正文。
- Produces: 模型主导主题、形式和场景数的 playback document；至少 3 Scene、五类实际存在类型的真实 source_ref 覆盖、每 Scene 一个循环外媒体尝试；循环结果统一为 `continue|complete|partial|failed`。

- [✅] 先实现通用 `bounded_loop` 静态 DAG 注册与运行边界；循环体不得包含 Tool、媒体、上传或发布副作用。
- [✅] 五类摘要按类型内稳定顺序、类型间轮转扫描全部合格素材；单轮切片由模型上下文与既有通用预算计算，禁止复用旧固定 refs 裁剪。
- [✅] 每轮模型输出通过 schema、source-ref allowlist、情绪安全和去重校验后再确定性 merge；未知引用、跨 Snapshot 引用和模型自造类型 fail-closed。
- [✅] 完成后校验每个实际存在的合格类型至少被一个 Scene 的真实 source_ref 引用；Business 缺安全 digest 时契约 fail closed。缺失类型只允许一次经相同 ModelGateway/预算/guardrail 的 repair，输入使用对应安全 digest 与真实 source_ref；无剩余许可/预算或仍缺失则 failed，Runtime 不用 deterministic 模板补写 Scene。
- [✅] 预算耗尽/局部批次失败且最终 `>=3`、覆盖成立时发布 partial；不能形成可播放文档才 failed。媒体失败只降低 enhancement，不触发文本整体 fallback。
- [✅] 回归 `1.0.0`～`1.0.3` 历史 3～8/16/80 合同与 `1.0.4` 放宽合同，确保新 evaluator 按锁定 package version 分流。

## Plan Self-Review

- Task 1 与 Task 2 都围绕 Runner，但 Task 2 依赖 Task 1 的安全素材字段，串行执行；Task 3 最后收敛真实工作流。
- 未引入正文持久化、动态工具、媒体任务或超出 MVP 的文案生成能力。
- Task 4 已在当前工作区实现并有自动化测试证据，不改变 Task 1～3 的历史完成证据；生产注册、部署与真实环境验证仍未执行，相应门禁保持未勾选。
